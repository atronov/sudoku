/**
 * Game settings.
 * Change them to tune the game.
 */
export const settings = {
    /**
     * This setting prevents putting a digit, that already present on horizontal, vertical or inside a square.
     */
    allowPutOnlyAvailableDigits: true,
    /**
     * The higher value - the easier
     */
    userToDigitDifficulty: {
        'easy': 48,
        'mid': 36,
        'hard': 28,
        'expert': 24,
    },
}
